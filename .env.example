# Homesphere — Environment variables (template)
# Copy to `.env.local` and fill in real values. `.env.local` is gitignored.

# ----- Supabase (public — safe to expose to browser) -----
NEXT_PUBLIC_SUPABASE_URL=https://ozkqrvenvxmfjblqjeon.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_NbmORTx3IMi9LrC_l-vHWw_7tzpAJ0C

# ----- Supabase (server-only — DO NOT expose) -----
# Service role key: bypasses RLS. Only for admin ops / seed scripts.
# Get from Supabase dashboard → Project Settings → API
SUPABASE_SERVICE_ROLE_KEY=TBD

# ----- Database (direct Postgres for Drizzle) -----
# Replace [PASSWORD] with your database password from Supabase dashboard.
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.ozkqrvenvxmfjblqjeon.supabase.co:5432/postgres
